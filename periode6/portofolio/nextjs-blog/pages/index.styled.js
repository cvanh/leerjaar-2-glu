import styled from "styled-components";

const Card = styled.div`
    height: 150px;
    width: 50px;
`

const Tumbnail = styled.div`

`


const Post = {
    Card,
    Tumbnail,
}

module.exports = Post